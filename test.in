#!/bin/sh

ID=$1
SRC=@PROJECT_SOURCE_DIR@
BIN=@PROJECT_BINARY_DIR@

EXEC=${BIN}/bin/${ID}
IN=${SRC}/data/${ID}.in
OUT=${SRC}/data/${ID}.out
ANS=${SRC}/data/${ID}.ans

${EXEC} < ${IN} > ${OUT}
diff ${OUT} ${ANS}
